var timer = 0;
var newTimer;
var active = false;

function start () {
  timer += 1;
  newTimer = timer;
  // setInterval(){
    return active = true;
  // }, 1000);
}

// function stop () {
//   return active = false;
// }

// stop();
